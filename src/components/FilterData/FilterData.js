import classNames from 'classnames/bind';
import styles from './FilterData.module.scss';

const cx = classNames.bind(styles);

const FilterData = () => {
    return <div className={cx('filter-wrapper')}></div>;
};

export default FilterData;
